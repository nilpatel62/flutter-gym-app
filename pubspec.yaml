name: form_coach
description: AI workout form coach
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.11.0+2
  permission_handler: ^12.0.1
  google_mlkit_pose_detection: ^0.14.0
  google_mlkit_commons: ^0.11.0
  vibration: ^3.1.5
  audioplayers: ^6.0.0
  supabase_flutter: ^2.5.6
  intl: ^0.19.0

flutter:
  uses-material-design: true
  assets:
    - assets/beep.mp3
